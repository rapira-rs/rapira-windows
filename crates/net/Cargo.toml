[package]
name = "rapira_net"
version = "0.8.0"
edition.workspace = true
publish = false
license = "MIT"
description = "Accept loop for rapira plugins - serves the listener that the server prepared"

[lints]
workspace = true

[dependencies]
extension_api = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true, features = ["rt", "rt-multi-thread", "net", "time", "macros"] }
