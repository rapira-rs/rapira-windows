[package]
name = "integration_tests"
version = "0.1.0"
edition = "2024"
publish = false
rust-version.workspace = true

[lints]
workspace = true

# The smoke test spawns the built `rapira.exe` and talks to it over TCP — nothing beyond std.
# `rapira.exe` (a root-package target) is compiled by the same `cargo test --workspace` build
# phase and sits under target\<profile>\ before any test binary runs.
[dependencies]

[dev-dependencies]
