# Extensions bundled into the Windows release (one php_<name>.dll each, from the
# official binary zip's ext\ dir). opcache is handled separately (zend_extension).
openssl
curl
mbstring
dom
xml
simplexml
xmlreader
xmlwriter
pdo_sqlite
sqlite3
fileinfo
phar
tokenizer
ctype
session
filter
iconv
